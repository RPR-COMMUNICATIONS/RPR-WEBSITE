# ⚓ MOTHERSHIP OS: COMMAND SURFACE (STRIKES) [v1.0.0]

**Authority:** THE OVERWATCH // ENGINEER ELDER  
**Status:** AUTHORITATIVE // LATCHED  
**Environment:** TS-Λ3 // SOVEREIGN_ROOT  
**Timestamp:** 2026-02-18T16:40:00Z  

---

## 🚥 CORE COMMANDS (Strikes)

Use these commands for local development, validation, and deployment.

- `npm run dev` – Local dev server (Vite). Primary workspace tool.  
- `npm run build` – CLEAN production build. Executes the Smallcaps Mandate before `tsc` and `vite build`.  
- `npm run verify` – Full mechanical confirmation sequence. Runs `local-validate.sh`, lint, and build. Required before any Production Strike.  
- `npm run lint` – Static code analysis and warning audit.  
- `npm run deploy` – Production deploy to `rprcomms.com` via `strike.sh`.  
- `npm run deploy:test` – Staging deploy to `rpr-corporate-site` via `test-strike.sh`.  
- `npm run clean` – Emergency substrate hygiene. Clears build caches and temporary artifacts.  
- `npm run server` – Financial backend. Initializes the local Stripe listener and Express logic.  

---

## 🛠️ MAINTENANCE PROTOCOLS

If the environment detects a **Casing Breach** or **Identity Drift**, execute the *Clinical Reset*:

```bash
npm run clean
npm run verify
npm run dev
```

---

**Audit Marker:** `COMMAND-SURFACE-v1.0.0-LATCHED`  
**Marker:** `[2026-02-18 16:40] // ENGINEER ELDER // STRIKE TOPOLOGY NORMALIZED.`  

**DOC_AUTHORITY // OVERWATCH SG-CANONICAL-2026**
```