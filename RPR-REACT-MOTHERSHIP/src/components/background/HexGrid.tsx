import React from 'react';

const HexGrid: React.FC = () => {
  return (
    <div className="absolute inset-0 z-0 opacity-[0.08] pointer-events-none">
      {/* This is a placeholder for the hexagonal grid background */}
    </div>
  );
};

export default HexGrid;
